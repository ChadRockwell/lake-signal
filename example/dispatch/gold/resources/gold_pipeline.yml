resources:
  pipelines:
    gold:
      name: "lakesignal_example_dispatch_gold"
      catalog: ${var.catalog}
      serverless: true
      target: ${var.schema}
      libraries:
        - file:
            path: ../transformations/gold_datamart.sql
